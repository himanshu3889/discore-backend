# TODO: NEED TO USE IN THE GATEWAY
routes:    
  - path: "/ws"
    auth: true
    module: "websocket"
    address: "http://localhost:8080"
    
  - path: "/chat/api/*"
    auth: true
    module: "chat"
    address: "http://localhost:8080"   
    
  - path: "/core/api/*"
    auth: true
    rateLimit: true
    module: "core"
    address: "http://localhost:8080"
